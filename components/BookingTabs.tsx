import { Text, View, StyleSheet, SafeAreaView } from "react-native";
import { Link } from "expo-router";
import { ScreenStyling } from "@/constants/Styles";

export default function BookingTabs() {
  return (
    <SafeAreaView style={{}}>
      <Text>Booking Tab</Text>
    </SafeAreaView>
  );
}
